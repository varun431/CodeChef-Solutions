#include <bits/stdc++.h>
#define ll long long
#define sc(n) scanf("%lli",&n)
using namespace std;

int main()
{
    ll t,*a,*b,*c,n;
    sc(t);
    while(t--)
    {
        sc(n);
        a = new ll[n];
        b = new ll[n];
        c = new ll[n];
        for(ll i=0; i<n; i++)
            sc(a[i]);sc(b[i]);sc(c[i]);
        for(ll i=0; i<n; i++)
        {
            for(ll j=0;j<)
        }
    }
}
